let paginate = require('./paginate');
let validate = require('./validate');
let change_alias_vi = require('./change_alias_vi');
let currentTime = require('./currentTime');

module.exports = {
	paginate: paginate,
	validate: validate,
	change_alias_vi: change_alias_vi,
	currentTime: currentTime
};